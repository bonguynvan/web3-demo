# ─── Frontend (Vite) ───────────────────────────────────────────────────────
# Vite only exposes vars prefixed with VITE_ to client code.
# Copy this file to .env.local and edit as needed.
# The defaults match the @perp-dex/server local dev ports.

# REST API base URL (no trailing slash)
VITE_API_URL=http://localhost:3001

# WebSocket server URL (price + trade event channels)
VITE_WS_URL=ws://localhost:3002
